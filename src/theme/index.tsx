const theme = {
	palette: {
		primary: {
			main: '',
		},
		secondary: {
			main: '',
		},
		default: {
			main: '',
		},
		paper: '#ffffff',
	},
	spacing: (value: number): string => {
		return `${value * 8}px`
	},
}

export default theme
